import './globals.css';

export const metadata = {
  title: 'THUSO Food',
  description: 'Order food and track delivery with THUSO.',
};

export default function RootLayout({children}) { return <html lang="en"><body>{children}</body></html>; }
